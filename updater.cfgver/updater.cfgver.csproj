<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <RootNamespace>updater.cfgver</RootNamespace>
    <AssemblyName>updater.cfgver</AssemblyName>
  </PropertyGroup>

  <ItemGroup>
    <EmbeddedResource Include="migration_rules.json" />
  </ItemGroup>
</Project>