# 待办事项 (TODO)

## 架构与功能优化
- [ ] **云控功能开关 (Feature Toggles / Kill Switch)**
  - **描述**: 在 `ServerCfg.json` 中配置动态功能开关，用于紧急禁用高危或存在严重 Bug 的功能（如扫描功能导致数据误删）。
  - **配置参考**: 增加 `featureToggles` 对象或 `disabledFeatures` 数组列表。
  - **UX/交互建议**: 
    - 针对禁用的功能入口不采取“隐藏”策略，而是采用“置灰（`IsEnabled = false`）”。
    - 当用户悬浮在被禁用的按钮上时，提供明确的 ToolTip 提示（例如：“当前功能维护中，请留意后续更新”或“为保护数据安全，此版本该功能临时禁用”）。
  - **安全策略**: 断网获取不到云端配置时的默认兜底行为（Fail-safe）。

## 界面与功能特性
- [ ] **全局公告系统 (Announcement System)**
  - **描述**: 在主界面搜索按钮左侧新增一个“公告”按钮，点击后弹出展示公告的 GUI 界面。
  - **云控配置**: 在云端配置文件（如 `ServerCfg.json`）中增加公告数据节点，必须支持同时下发/展示多条公告。
  - **交互建议**: 
    - 有新公告时，按钮可展示红点或角标提示，每条公告添加已读按钮。
    - 列表展示多条公告（可带时间、标题、正文等字段）。