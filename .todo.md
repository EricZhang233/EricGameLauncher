# 待办事项 / To-Do List

## 待继续 (ToBeContinue)

**【目前正在开发的功能】**
- **显示 GitHub Release 的 Markdown 更新日志弹窗**：当用户检查更新或发现新版本时，弹出一个包含 WebView2 的 ContentDialog，用于渲染并展示远程拉取的 Markdown 格式更新日志。

**【项目背景与当前状态】**
- 弹窗 UI 采用 WinUI 3 `ContentDialog` + `WebView2`。
- 之前为了排查网页裁剪和不居中问题，**已移除了所有的**手动排版限制和强制尺寸约束，代码已回归最纯净的状态。
- 但由于 WinUI 3 原生默认的 `ContentDialogMaxWidth` 极小（约 500px），导致现在的弹窗变成了一个严重压缩的小方块。

**【接下来的任务】**
- 不要给 `WebView2` 写死尺寸锁定限制。应当动态获取当前窗口大小。
- 必须通过注入/覆盖系统内置尺寸资源（如 `Resources["ContentDialogMaxWidth"]` / `Resources["ContentDialogMaxHeight"]`）来放大弹窗承载能力。
- 最终要确保 Markdown 的框体不仅放大，内容能在框架下居中并不被异常裁切。

**【⛔ 绝对禁止的操作】**
- **禁止使用终端脚本（如 PowerShell）向文件写入代码或替换等！** 会破坏 Diff 审查！
- 所有的代码编辑**必须**使用标准的专门文本编辑工具（如 `replace_string_in_file`），并附带3-5行上下文，保障最终的人工改动追溯审查体验不变。

**清单：**
- [ ] 查阅 MainWindow.xaml.cs 中更新弹窗代码（特别是 `ContentDialog` 初始化部分）目前的现状。
- [ ] 严格使用标准工具注入 `ContentDialogMaxWidth` 等资源尺寸的重写覆盖策略。
- [ ] 确保 WebView2 内容宽敞居中且未被裁剪。

---

## 近期计划 (Planned)

- [feat] 区分稳定版（每个版本号第三位为[10]的倍数的版本(如x.x.10、x.x.20)将被视为稳定版，稳定版可能存在第四位版本号(如x.x.10.x)）和开发版（最新版本号）
---

## 目前未排期区域 (Currently Unscheduled)

暂无。
