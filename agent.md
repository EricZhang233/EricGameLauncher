# 仓库开发规则

## 1. 无注释原则（全链路强制）
代码中禁止出现任何形式的注释（含 `//`、`/* */`、`<!-- -->` 等）。
删除代码时直接移除源码，严禁以注释方式保留。
项目内所有文件（含既有及后续修改）均不得包含注释。
构建与工程配置文件（如 .csproj）中的 XML 组织性注释 `<!-- -->` 不视为违反本原则。

## 2. 文件修改规则（全链路强制）
禁止使用任何编辑器工具自带工具以外的手段修改项目文件，禁止使用脚本辅助修改。
终端命令修改文件绕过了逐行可审查性，无法在编辑器中追踪差异，不符合代码审查要求。

## 3. 运行规则（全链路强制）
当我说run的时候，你需要编译debug并启动，这是永久守则

## 4. 日志系统（全链路强制）
当加入新功能\模块或修改现有功能\模块时，需要兼容日志系统，确保相关操作被记录在日志中。

## 5. Release Note 规则（全链路强制）
项目根目录下 `.releasenoteguide.md` 为编写规范，`.releasenote.md` 为实际发布说明。
当提及 "note" / "release note" / "更新日志" / "发版说明" 时，必须：
- 先读取并遵循 `.releasenoteguide.md` 的格式、分类、粒度要求
- 再对比当前与目标提交的差异，增写至 `.releasenote.md`
- 面向用户而非开发者，省略技术实现细节与内部 API 名称
- 不暴露 Debug/开发后门相关内容

## 6. 全功能 CLI 支持原则（全链路强制）
所有功能(包括以后新增)须在 CLI 下完整可用，并通过 `-help` 参数输出完整帮助信息。
发布目录根目录附带 `SKILL.md`，供 Agent 接入 CLI；完整接入文档嵌入 CLI，通过 `skill` 命令输出，杜绝篡改。
所有面向外部的文档均通过 `-help` 与 `skill` 命令获取，发布目录不再附带明文说明文档。
CLI功能需深度整合到每一个操作，禁止存在GUI独占功能。
Help 输出与 `skill` 命令内置文档须随功能变更同步自动维护，无需开发者专门提醒！

## 7. 全量 Core 化原则（全链路强制）
main.exe 和 cli.exe 为零业务逻辑的纯壳层。
MainWindow 仅负责 WinUI 渲染（XAML 控件、动画、对话框、窗口消息）。
CliService 仅负责控制台 I/O（参数解析、输出格式化、颜色）。
两壳层之间、壳层与 Core 之间禁止任何平行实现。
所有业务逻辑必须单一存在于 Core 层服务类中，被两壳层共用调用。
新增功能也必须遵循此原则，先 Core 后壳，禁止在壳层先行实现。

## 8. 禁止文本硬编码原则（全链路强制）
所有文本必须走text.cs，存储于text.yaml中。
品牌标识（应用名、开发者署名）与图形占位符不视为违反本原则。