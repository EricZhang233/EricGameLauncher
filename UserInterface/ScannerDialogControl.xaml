<ContentDialog
    x:Class="EricGameLauncher.ScannerDialogControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:models="using:EricGameLauncher">
    <Grid MinWidth="450" MinHeight="350">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,16" ColumnDefinitions="*,Auto">
            <StackPanel Grid.Column="0" VerticalAlignment="Center" Spacing="4" Margin="0,0,12,0">
                <TextBlock x:Name="ScannerDialogTitle" Text="" FontSize="20" FontWeight="SemiBold"/>
                <TextBlock x:Name="ScannerDescriptionText"
                           Text=""
                           Style="{ThemeResource CaptionTextBlockStyle}"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                           TextWrapping="Wrap"/>
            </StackPanel>
            <Button Grid.Column="1" Background="Transparent" BorderThickness="0" VerticalAlignment="Top"
                    x:Name="ScannerDialogCloseBtn" Click="ScannerDialogCloseBtn_Click" Height="36" Width="36" Padding="0">
                <FontIcon Glyph="&#xE711;" FontSize="14"/>
            </Button>
        </Grid>

        <Grid x:Name="ScannerLoadingPanel" Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center">
            <StackPanel Spacing="16">
                <ProgressRing Width="48" Height="48" IsActive="True"/>
                <TextBlock x:Name="ScannerLoadingText" Text="" HorizontalAlignment="Center" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
            </StackPanel>
        </Grid>

        <ScrollViewer x:Name="ScannerResultPanel" Grid.Row="1" Visibility="Collapsed" MaxHeight="450">
            <StackPanel Spacing="24" Margin="0,0,12,0">
                <StackPanel x:Name="ScannerNewGamesSection">
                    <Grid Margin="0,0,0,8">
                        <TextBlock x:Name="ScannerNewGamesHeader" Text="" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center"/>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Spacing="8">
                            <Button x:Name="ScannerNewSelectAllBtn" Content="" FontSize="12" Padding="8,4" Click="ScannerNewSelectAllBtn_Click"/>
                            <Button x:Name="ScannerImportSelectedBtn" Content="" Style="{StaticResource AccentButtonStyle}" FontSize="12" Padding="8,4" Click="ScannerImportSelectedBtn_Click"/>
                        </StackPanel>
                    </Grid>
                    <ListView x:Name="ScannerNewGamesList" SelectionMode="Multiple" ScrollViewer.VerticalScrollMode="Disabled" ItemContainerStyle="{StaticResource DefaultListViewItemStyle}" SelectionChanged="ScannerNewGamesList_SelectionChanged">
                        <ListView.ItemTemplate>
                            <DataTemplate x:DataType="models:ScannedGame">
                                <Grid Margin="0,4" ColumnDefinitions="*,Auto">
                                    <TextBlock Grid.Column="0" Text="{x:Bind Title}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                                    <Border Grid.Column="1" Background="{ThemeResource AccentFillColorDefaultBrush}" Padding="6,2" CornerRadius="4" Margin="12,0,0,0">
                                        <TextBlock Text="{x:Bind PlatformBadge}" FontSize="10" Foreground="White" FontWeight="Bold"/>
                                    </Border>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </StackPanel>

                <StackPanel x:Name="ScannerExistingGamesSection">
                    <TextBlock x:Name="ScannerExistingGamesHeader" Text="" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,8"/>
                    <ListView x:Name="ScannerExistingGamesList" SelectionMode="None" ScrollViewer.VerticalScrollMode="Disabled" Opacity="0.6">
                        <ListView.ItemTemplate>
                            <DataTemplate x:DataType="models:ScannedGame">
                                <Grid Margin="0,4" ColumnDefinitions="*,Auto">
                                    <TextBlock Grid.Column="0" Text="{x:Bind Title}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                                    <Border Grid.Column="1" Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" Padding="6,2" CornerRadius="4" Margin="12,0,0,0">
                                        <TextBlock Text="{x:Bind PlatformBadge}" FontSize="10" Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontWeight="SemiBold"/>
                                    </Border>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </StackPanel>

                <StackPanel x:Name="ScannerInvalidGamesSection">
                    <Grid Margin="0,0,0,8">
                        <TextBlock x:Name="ScannerInvalidGamesHeader" Text="" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center"/>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Spacing="8">
                            <Button x:Name="ScannerInvalidSelectAllBtn" Content="" FontSize="12" Padding="8,4" Click="ScannerInvalidSelectAllBtn_Click" Foreground="IndianRed"/>
                            <Button x:Name="ScannerDeleteInvalidBtn" Content="" FontSize="12" Padding="8,4" Click="ScannerDeleteInvalidBtn_Click" Foreground="White" Background="IndianRed"/>
                        </StackPanel>
                    </Grid>
                    <ListView x:Name="ScannerInvalidGamesList" SelectionMode="Multiple" ScrollViewer.VerticalScrollMode="Disabled" ItemContainerStyle="{StaticResource DefaultListViewItemStyle}" Opacity="0.8" SelectionChanged="ScannerInvalidGamesList_SelectionChanged">
                        <ListView.ItemTemplate>
                            <DataTemplate x:DataType="models:ScannedGame">
                                <Grid Margin="0,4" ColumnDefinitions="*,Auto">
                                    <TextBlock Grid.Column="0" Text="{x:Bind Title}" Foreground="IndianRed" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" TextDecorations="Strikethrough"/>
                                    <Border Grid.Column="1" Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}" BorderBrush="IndianRed" BorderThickness="1" Padding="6,2" CornerRadius="4" Margin="12,0,0,0">
                                        <TextBlock Text="{x:Bind PlatformBadge}" FontSize="10" Foreground="IndianRed" FontWeight="SemiBold"/>
                                    </Border>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</ContentDialog>
