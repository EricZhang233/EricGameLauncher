<UserControl
    x:Class="EricGameLauncher.RecycleBinFlyoutControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:models="using:EricGameLauncher">
    <StackPanel x:Name="RootPanel" Width="340" Spacing="12" Padding="16">
        <Grid ColumnDefinitions="*,Auto" VerticalAlignment="Center">
            <TextBlock x:Name="RecycleTitle" Text="" FontSize="20" FontWeight="SemiBold"/>
            <Button x:Name="BtnEmptyRecycleBin" Content="" Grid.Column="1" Click="BtnEmptyRecycleBin_Click"/>
        </Grid>
        <TextBlock x:Name="RecycleDescription" Text="" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Style="{ThemeResource CaptionTextBlockStyle}" TextWrapping="Wrap"/>
        <ListView x:Name="RecycleItemsControl" MaxHeight="400" ScrollViewer.VerticalScrollBarVisibility="Auto" SelectionMode="Single">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem" BasedOn="{StaticResource DefaultListViewItemStyle}">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Margin" Value="0,0,0,4"/>
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="CornerRadius" Value="6"/>
                    <Setter Property="Background" Value="{ThemeResource CardBackgroundFillColorDefaultBrush}"/>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:AppItem">
                    <Grid Padding="10" Background="Transparent" RightTapped="RecycleItem_RightTapped">
                        <Grid.ContextFlyout>
                            <MenuFlyout Opening="RecycleMenuFlyout_Opening">
                                <MenuFlyoutItem Text="" Icon="Undo" Click="MenuRestore_Click" Tag="{Binding}"/>
                                <MenuFlyoutItem Text="" Icon="Delete" Click="MenuDeletePerm_Click" Tag="{Binding}"/>
                            </MenuFlyout>
                        </Grid.ContextFlyout>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="32"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0" Width="28" Height="28" CornerRadius="6" Background="{ThemeResource LayerOnAcrylicFillColorDefaultBrush}">
                            <Image Source="{Binding IconPath, Converter={StaticResource ImagePathConverter}, TargetNullValue={x:Null}}" Stretch="Uniform" Margin="2"/>
                        </Border>
                        <TextBlock Grid.Column="1" Text="{Binding Title}" TextDecorations="{Binding TitleTextDecorations}" VerticalAlignment="Center" Margin="10,0,0,0" Style="{ThemeResource BodyTextBlockStyle}" TextTrimming="CharacterEllipsis"/>
                        <Border Grid.Column="2" CornerRadius="4" Padding="6,2" VerticalAlignment="Center" Background="{ThemeResource SystemFillColorCriticalBackgroundBrush}" Visibility="{Binding TimeBadgeVisibility}">
                            <TextBlock Text="{Binding TimeRemainingText}" FontSize="12" FontWeight="SemiBold" Foreground="{ThemeResource SystemFillColorCriticalBrush}"/>
                        </Border>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackPanel>
</UserControl>
