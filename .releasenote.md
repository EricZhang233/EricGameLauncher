本次更新聚焦启动速度：新增「快速启动」，让启动器以无界面后台服务常驻，并可被瞬时唤醒。

---

## ✨ 新功能（Features）

- **快速启动**：
  - 设置 → 常规首项新增「快速启动」开关（默认开启），开启后启动器以服务方式注册登录自启，登录后静默运行且不显示任何界面
  - 开启快速启动时，从桌面或开始菜单图标启动会直接唤醒后台已预热的窗口，启动几乎瞬时完成
  - 开启快速启动后，关闭窗口不再结束程序，而是转入无界面后台运行，便于随时瞬时唤起
  - 关闭该开关会立即移除自启项，并恢复「关闭窗口即退出」的原行为
  - 命令行同样可用：`settings --set quickStart=true|false` 开闭，`settings --list` 查看当前自启状态与命令；`uninstall` 会一并清理自启项  
  - 开启快速启动期间，启动器进程会持续驻留于后台（属预期行为）
    - 需要暂时彻底退出时，可在任务栏右键选择「结束任务」
    - 该菜单项适用于 Windows 11 24H2 及以上；若未显示，可在「设置 → 系统 → 开发者选项」中开启「结束任务」
