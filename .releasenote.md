本次更新聚焦界面架构与启动流程：将原本集中在单一窗口中的界面拆分为独立组件、主窗口收敛为纯外壳，并引入启动过渡界面。

---

## ✨ 新功能（Features）

- **启动过渡界面**：
  - 启动时先呈现由应用图标、应用名与加载指示组成的过渡画面，取代初始化期间的空窗等待
  - 在「界面已加载 + 数据就绪 + 界面就绪」三项条件全部满足后，以 500ms 淡出动画退场
  - 设置 10 秒强制兜底：即使初始化异常或超时，也会强制进入主界面，不会停留在过渡画面

## 🛠️ 优化（Improvements）

- **启动响应**：
  - 启动后窗口立即出现并显示过渡画面，不再有「点击后长时间没有窗口」的等待期
  - 主界面界面的解析、控件装配、本地化与设置加载推迟至窗口激活后执行，窗口构造阶段只构建轻量占位内容

## 🐛 缺陷修复（Bug Fixes）

- 修复在搜索结果筛选状态下调整图标顺序，导致列表未按新顺序显示的问题
- 修复重新添加已移入回收站的游戏时不判定为重复，导致同一游戏出现重复条目的问题

## 🗂️ 内部调整（Internal）

- **界面层组件化拆分**：`MainWindow` 由 3107 行代码 + 1473 行 XAML 收敛为 308 行代码 + 33 行 XAML，界面切分为 11 个独立 `UserControl`，统一收纳至新增的 `UserInterface/` 目录
  - `MainView`：主视图（标题栏、图标网格、菜单与各控件编排）
  - `PropertyPanelControl`：属性面板
  - `SearchFlyoutControl`、`IconSizeFlyoutControl`、`OrderFlyoutControl`、`RecycleBinFlyoutControl`、`SettingsFlyoutControl`、`AnnouncementFlyoutControl`：各功能浮出
  - `ScannerDialogControl`：扫描对话框
  - `EmptyStateControl`：空状态
  - `MigrationOverlayControl`：迁移遮罩
- **窗口与视图职责分离**：`MainWindow` 仅保留窗口外壳职责（启动浮层、窗口状态与位置、标题栏与背景、WndProc、窗口图标、关闭清理、单实例聚焦）；`MainView` 承担全部界面与交互，对外暴露 `Start(window)` / `ActivateAndFocus()`，并以 `MarkDataReady()` / `MarkUiReady()` 回驱动窗口隐藏启动浮层
- **组件按需创建**：`SettingsFlyoutControl`、`RecycleBinFlyoutControl`、`ScannerDialogControl` 改为首次使用时实例化，事件订阅与本地化同步在该时机完成，降低启动期开销
- **启动时序调整**：`App.OnLaunched` 在 `Activate()` 之后经 `DispatcherQueue` 延迟调用 `StartInitialization()`，把界面构建与数据加载移出窗口构造路径
- **数据模型与 WPF 类型解耦**：移除 Core 层 `AppItem.DisplayIcon`（`BitmapImage`）属性，图标位图完全由界面层持有
- **路径归一化收口**：抽出 `PathHashHelper.NormalizePath`，哈希生成与重复判定共用同一归一化实现
- **重复判定增强**：`ItemService.CheckDuplicate` 将回收站条目纳入比对池，新增空路径短路，并在 ID 比对之外追加 `ExePath` 归一化比对
- **日志衔接**：窗口准备、视图装配、启动浮层隐藏/超时等启动关键节点写入 `Startup` 日志，重复判定、排序刷新与组件创建写入 `App`/`UI` 日志，各组件均以 `using (LogService.StartOperation(...))` 包裹主流程
