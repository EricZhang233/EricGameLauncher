# Eric Game Launcher (EGL) **v1.0.10.3** - 代码结构优化

重构样式表内嵌方式、合并更新弹窗 UI 逻辑，优化通道说明布局，并将更新渠道分支判断改为基于 GitHub Pre-Release 标签。

> [!IMPORTANT]
> **此版本为稳定版**
> 稳定版将致力于使各项新增功能趋于稳定，不增加新功能。

---

### UI 优化 (UI Improvements)

- **通道说明脚注位置优化**
  将更新弹窗中的通道说明从日志区域下方移至按钮行正上方居中显示，日志可视区域恢复完整高度，不再因脚注占位而被压缩。

- **设置页迁移配置按钮精简为仅图标**
  移除按钮内的文字标签，改为鼠标悬停时通过 Tooltip 显示对应操作说明（"切换到便携模式"或"切换到系统模式"），与同行的打开文件夹按钮风格保持一致。

---

### 功能调整 (Changes)

- **更新渠道分支判断策略调整**
  稳定版渠道现在只推送 GitHub 上**未标记 Pre-Release** 的正式版本，开发版渠道推送包含 Pre-Release 在内的最新版本。

---

### 代码重构 (Refactoring)

- **CSS 样式表完全内嵌至 `Core.cs`**
  将原先作为嵌入资源的 `webview.css` 文件内容直接以 C# 原始字符串字面量（`"""`）硬编码为 `WebViewStyles.MarkdownCss` 常量，消除运行时资源流读取，并从 `.csproj` 中移除对应的 `EmbeddedResource` 条目。

- **合并"有更新"与"无更新"弹窗 UI 逻辑**
  提取共享辅助方法 `BuildReleaseContentAsync`（负责 WebView2 初始化、HTML 拼接、Grid 布局）与 `ShowReleaseDialogAsync`（负责弹窗配置与按钮差异），两种场景均通过 `hasUpdate` 参数区分：有更新时显示"确认更新"主按钮，无更新时显示"强制重装"次按钮，消除约 100 行重复代码。

- **整理 `Core.cs` 的 region 结构**
  将 `LauncherConstants` 与 `WebViewStyles` 纳入 `#region Base Infrastructure`，使命名空间内所有顶级类均有归属，region 边界整齐一致。

- **更新分支判断改为基于 GitHub `prerelease` 标签**
  废弃原先通过版本号第三段是否为10的倍数来区分稳定版的脆弱规则，改为直接读取 GitHub Releases API 返回的 `prerelease` 字段：有 Pre-Release 标签的为开发版，无标签的为正式版。两个渠道均从全量列表（`/releases?per_page=100`）获取数据，稳定版渠道过滤 `prerelease == false`，开发版渠道取版本号最大的一条（含 pre-release）。同步移除已无用的 `IsStableVersion` 方法与 `/releases/latest` 端点常量。

### 已移除 (Removed)

- **删除 `webview.css` 文件**
  样式表已完全内嵌，外部文件不再需要。
