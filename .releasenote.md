本次更新聚焦于启动后自动关闭行为优化。
_此版本更新内容过少，故作为下一版本的Pre版发布，Pre版将累计更新且不发布正式版。_
[no-release]

### 🛠️ 优化（Improvements）
- **启动后自动关闭**：当启用“启动后关闭”时，新增 15 秒延迟自动退出机制；在检测到窗口仍为前台或有用户鼠标/键盘活动时会取消退出，避免意外关闭。
- **启动渲染稳定性**：在程序启动时延迟展示主应用网格（`AppGrid`）并在根可视树加载后（约 600 ms）再执行首次刷新；同时在首次刷新前对 `RefreshView()` 与 `UpdateGridItemSizes()` 加入就绪闸控，避免 WinUI/WinUI XAML 在初始模板批量实例化时触发的原生渲染崩溃（已在启动日志中增加对应记录）。
- **设置界面路径图标区分**：修正设置界面中“打开配置”和“打开缓存”两个路径按钮使用相同图标的问题；将缓存目录按钮图标更改为更具区分性的文件夹图标，提升界面可识别性。
- **图标选择器（文件筛选）**：在“更改图标”对话中新增两类筛选。默认“可执行 + 图像”，以及“所有文件”；已补全合并分组本地化命名，以保证各语言下的显示一致性与可读性。
- **日志系统**：日志系统全面覆盖，为后续debug提供支持。
 
### 🐛 缺陷修复（Bug Fixes）
- **属性面板 — 更改图标异常**：修复在属性面板通过“更改图标”选择图片时，返回系统缩略图或不正确图像的问题。对常见图片格式（png/jpg/bmp 等）会直接转换并缓存为 PNG，以保证预览一致性；对 `.ico` 和可执行文件（`.exe`）则保留图标提取流程。
- **更多选单 — UI**：更多选单内启动器logo消失问题已修复。
- **设置界面 — 语言设置**：修复了设置界面内语言设置显示与实际应用不一致的问题。

 
### 💥 破坏性变更（Breaking Changes）
- **i18n 精简**：为降低维护成本，本版本起将仅保留简体中文（Zh-CN）、英文（EN）、繁体中文（Zh-TW）和日文（JA）的语言支持，其他语言将被从程序内删除。升级后，使用已移除语言环境的用户界面将回退至默认语言。
