# Eric Game Launcher (EGL) **v1.0.10.2** - 设置优化与稳定性修复

新增更新通道选择与弹窗脚注，重构设置浮层布局并修复多处弹窗尺寸、i18n 及代码质量问题。

> [!IMPORTANT]
> **此版本为稳定版**

> [!NOTE]
> 每个版本号第三位为[10]的倍数的版本(如x.x.10、x.x.20)将被视为稳定版，稳定版可能存在第四位版本号(如x.x.10.x)。
> 稳定版将致力于使各项新增功能趋于稳定，不增加新功能。
---
### 功能新增 (New Features)

- **新增更新通道选择**
  在「设置」中新增"更新通道"下拉框，支持在**稳定版**与**开发版**之间切换，配置持久化保存至 `config.json`，默认初始化为稳定版。
  - **稳定版**：以稳定性为优先，通常不包含最新功能。
  - **开发版**：始终跟进最新 GitHub 发行版，可能含有尚未确认的缺陷。
  - 设置面板新增通道含义说明文字（全部 8 种语言均已 i18n 化）。

- **更新弹窗新增通道说明脚注**
  "检查更新"与"发现新版本"两个弹窗底部（按钮上方）均新增当前通道提示，样式为辅助文字，内容为当前通道名称及跳转设置的引导（全部 8 种语言均已 i18n 化）。

---
### UI 优化 (UI Improvements)

- **设置浮层全面重构布局**
  - 浮层宽度固定为 300px，排版更紧凑统一。
  - "更新通道"由 `ToggleSwitch` 改为 `ComboBox` 下拉框，与标题文字居于同一行。
  - "启动程序后自动退出"与其开关合并为单行布局，与其他设置项样式一致。
  - "启动程序后自动退出"与"更新通道"各新增语义图标（分别使用 Segoe Fluent Icons `&#xE8BB;` / `&#xE895;`）。
  - 同类设置项之间添加细分割线（1px），不同大节之间使用粗分割线（2px）以建立视觉层级。
  - 存储路径文字移除，改为绑定至"存储模式"标签的鼠标悬停提示（ToolTip），减少界面占用。

---
### Bug 修复与稳定性强化 (Bug Fixes & Improvements)

- **修复"检查更新"弹窗布局问题**
  修复了更新日志弹窗因 WinUI 3 内置 `ContentDialogMaxWidth` 限制（约 500px）而被严重压缩为小方块的问题。现在弹窗会动态读取当前窗口尺寸，按比例（宽 80%、高 78%）自适应展开，WebView2 内容区域也随之正确填满，不再出现异常裁切。

- **修复"发现新版本"弹窗缺少动态尺寸适配**
  `StartUpdateFlowAsync` 弹窗此前无动态尺寸逻辑，现与"检查更新"弹窗统一，按当前窗口尺寸比例（宽 80%、高 78%）自适应，并正确覆盖 `ContentDialogMaxWidth / ContentDialogMaxHeight`。

- **修复更新检测结果显示位置错误**
  "您当前使用的是最新版本"等检测结果文字此前被错误地渲染在 Markdown 正文区域内（`<h2>` 标签），现已移至弹窗标题栏正确显示，正文区域仅保留版本更新日志内容。

- **修复多处国际化（i18n）缺失与错误**
  - 修复代码中引用了不存在的 i18n 键 `Button_Save` / `Button_Delete` 的问题（已更正为 `Property_Save` / `Menu_Delete`）。
  - 为繁体中文（Zh-TW）补全了缺失的 `Update_Dialog*` / `Update_NoUpdate*` 系列共 6 个键。
  - 为日语（JA）补全了缺失的 `Menu_SystemIntegration`、`Menu_Install`、`Menu_Uninstall`、`Menu_CheckUpdate` 及 Update 系列共 10 个键。
  - 为韩语（KO）、法语（FR）、德语（DE）、西班牙语（ES）各补全了缺失的 `Menu_CheckUpdate`、`Property_CustomItem` 及 Update 系列共 7–8 个键。

---
### 代码质量 (Code Quality)

- **全项目注释清理**
  清除主项目（`Core.cs`、`MainWindow.xaml.cs`）及子项目（`updater.main`、`updater.cfgver`）中所有 `//` 行内注释，保留 URL 字符串字面量不受影响。

- **移除冗余字段 `_toggleCloseAfterLaunch`**
  该私有字段依赖 `Toggled` 事件触发后才赋值，导致 `LoadSettings()` 在首次调用时静默失效。现统一直接引用 XAML 命名元素 `ToggleCloseAfterLaunch`，消除不一致性。

- **移除无效的 `Console.WriteLine` 调用**
  WinUI3 应用无控制台窗口，`Core.cs` 中 updater 启动失败的 catch 块内 `Console.WriteLine` 永远不会显示，已替换为静默 `catch {}`。

- **清理 UI 代码中的冗余逻辑**
  移除了排序浮层 `EditOrderFlyout_Opening` 中错误放置的属性面板控件赋值代码（这些控件由 `ApplyLocalization()` 统一管理，重复赋值无意义）；同时将 XAML 中 `ScannerDialog` 区域的硬编码中文占位符全部改为空字符串。

