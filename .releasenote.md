本次更新重点修复了由于底层环境识别问题导致的第三方输入法中文候选框无法显示或跟随光标的顽固兼容性缺陷，同时深入重构了启动器自我更新时的跨进程生命周期调度机制，消除了原自动升级流程中的冲突崩溃隐患。

### 体验优化与缺陷修复 (Improvements & Fixes)

- **修复第三方输入法候选框无法正常显示的问题**
  解决了用户在使用微信、搜狗等第三方输入法时，由于中文候选框丢失或无法跟随光标而导致的输入障碍。现在全局搜索和配置页面等所有的文本输入框均能稳定、精准地呼出候选词，恢复了正常的输入交互体验。
  > **技术细节**：在 `app.manifest` 清单文件中补齐了完整的 Windows 10/11 操作系统兼容性声明，修正了识别降级误判，从而唤醒了底层 TSF (Text Services Framework) 对 WinUI 3 未打包模式的绘制支持。同时全面剥离了极易引发冲突的输入指针钩子 `InputNonClientPointerSource`，结合 WinAppSDK 的声明式拖放控制，打通并归还了原生输入焦点（HWND）在 WinUI 层的光标位置同步通路。

- **优化后台自动更新机制**
  重新梳理并改进了启动器的自我更新流程，解决了偶尔因应用退出时机不当导致的升级冲突或失败问题，使版本的迭代更迭更加平稳可靠。
  > **技术细节**：修改了主程序的终结与退出策略。修复了原自更新机制中，主应用拉起 `updater.main` 后自身提前终结导致的生命周期竞态条件（Race Condition）与进程冲突问题。现将进程终结权与文件换代操作完全移交至独立的更新器执行，以确保文件锁的安全释放与平滑覆盖。