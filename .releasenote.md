本次版本以「两段式回收站机制」与「数据安全防误删防护」为核心，辅以下层数据结构抽象精致打磨。

### 🚀 新功能 (Features)
- **全新回收站机制 (两段式防误删)**：
  - 主界面与多处列表下的“删除”及“清理失效项”操作现已统一变更为“移入回收站”（软删除），不再直接彻底抹除本地数据。
  - 新增独立「回收站」管理界面，允许用户集中查看已删除的项目，并提供快速恢复操作，有效防止因误操作导致的数据丢失。
- **72 小时保留倒计时保护**：
  - 仅在回收站内再次对项目执行“彻底删除”或点击“清空回收站”时，项目才会正式进入 72 小时真删除保留倒计时队列。
  - 倒计时保留期间，项目列表会展示醒目的红色剩余时间（如 71h）徽章及删除线视觉反馈，该阶段内依然可随时执行“恢复”。
  - 超过 72 小时仍未恢复的项目，系统将在每次启动应用时自动进行后台静默清理。

### 🛠️ 通用优化 (Optimizations)
- **底层架构升级与数据安全**：
  - 优化了列表排序与刷新加载的底层逻辑，确保拖拽交互能完美兼容并隐式保护已被分类至回收站的数据，保障数据生命周期闭环的安全。
  - 提升应用项目本地 JSON 序列化性能，为状态标记与倒计时字段专门增加动态忽略（JsonIgnore）机制，避免冗余属性膨胀配置文件体积。
  - 配置文件版本已升级，并在启动时自动迁移旧数据到新的回收站独立区块，确保升级过程无感且数据完整。
