Eric Game Launcher (EGL) **v1.0.6**

- **自定义右键菜单**
    - 支持为每个项目增加多达 10 个自定义菜单项。
    - 可配置菜单标题、命令路径及参数，并支持管理员权限运行设置。
    - 具备智能排位机制：删除中间项后，后续项目自动补位，确保配置列表整洁。

- **视觉与 UI 优化**
    - 菜单图标升级：自定义项使用 **标签 (Tag)** 图标，运行管理器使用 **扳手 (Repair)** 图标，功能区分更直观。
    - 属性面板重构：UI 布局再次重构风格完全对齐，间距与交互逻辑更加统一。
