# Eric Game Launcher (EGL) **v1.0.12** - 属性面板大重构，视觉风格焕新

将主窗体背景升级为通透的 DesktopAcrylicBackdrop 材质，并把属性面板全面重构为标准浮层视图以解决长期存在的排版问题。同时解锁了全格式文件拾取权限，完善了图标尺寸调节及相关体验修复。

> [!Important]
> **此版本为稳定版**

---

### UI 优化 (UI Improvements)

- **风格焕新**
  主窗体视觉风格由原来的`Mica`替换为`DesktopAcrylicBackdrop`，视觉更通透，界面更美观。移除了标题栏叠层，现在界面风格更统一。
- **属性面板视觉大焕新**
  **属性浮层特性加入**：我们觉得原有的属性面板会侵占主窗口显示控件，且由于需要避让属性面板导致我们在主视图显示开发上一直有难以攻克的边距异常bug，所以我们放弃了原有的面板视图，转向更符合WinUI3标准的浮层视图，这极大的提高了美观性及开发稳定性，同时也为以后的可扩展性打下坚实基础。此改动之后，我们的所有交互页面全部实现了浮层化。
  **自定义右键菜单区域视觉重构**：摒弃扁平化堆砌，引入“卡片嵌套”层级设计。整个自定义菜单区域被大卡片底色包裹，内部10个定义槽位分别采用次级卡片底色叠加极细描边，大幅增强长表单的立体维度与区隔度。

---

### 功能增强 (Features)

- **添加图标尺寸精细微调**
  在顶栏的图标大小调节浮层右上角增加了快捷微调按钮，现在除了拖拽滑动条外，您可以直接点击按钮，以 `1` 为步进单位对库中的图标大小进行高精度调节。
- **解除选取器 (FilePicker) 文件拦截与文本更正**
  正式解除了底层 `Win32FileDialog.ShowOpenFileDialog` 对可执行文件 (`.exe; *.lnk; *.url`) 的强制过滤，开放所有格式 (`*.*`) 的拾取权限。玩家现可将本地攻略记录、Mod工具、自定义文档甚至脚本作为并行启动项挂载。并配合该机制更正了带有误区引导的原生窗口标题（由 "Select Executable File" 更正为通用含义的 "Select file"）。

---

### 问题修复 (Bug Fixes)

- **修复极端情况下的图标卡片裁切**
  修复了由于自适应网格计算偏差，在窗口外部尺寸极小而设定图标尺寸过大时，导致图标左右两侧被强制遮挡截断的问题（优化了排列逻辑，确保项目的左右间距绝不被挤压至小于上下间距）。
- **修复更新状态指示器滞留**
  修正了应用更新检测逻辑。过去“发现新版本”的提示语及红点仅在冷启动时获取并锁定；现在当用户执行“手动检查更新”或在设置中“切换更新检查分支（Channel）”时，应用会立即重置当前指示器状态并重新发起云端拉取，避免出现状态断层。