# Eric Game Launcher (EGL) **v1.0.11** - 自适应网格重构与全格式文件拾取

重构主界面Grid视图以实现自适应间距，避免右侧在图标大小与窗口尺寸不匹配时大量留白，解锁了全局任意类型文件拾取能力。

> [!Important]
> **此版本为开发版**
> 包含最新特性或结构重构的实验性版本。

---

### UI 优化 (UI Improvements)

- **引入全新自适应网格 (AdaptiveGridView)**
  移除了原生计算存在边缘损耗和留白的 `GridView` 容器，全面引入并替换为 `CommunityToolkit.WinUI.UI.Controls` 库中的 `AdaptiveGridView`，实现空间完美拉伸与对齐分布，彻底消除右侧大面积空轨黑带。
- **属性面板层级与抽屉动画优化**
  重新定义了右侧滑出属性面板的图层权重层次，并调整动画缓动曲线（EasingFunction），视觉推挤与弹回效果更加自然流畅。

---

### 功能增强 (Features)

- **移除 FilePicker 的文件类型限制**
  解除了 `Win32FileDialog.ShowOpenFileDialog` 针对执行文件 (`.exe; *.lnk; *.url`) 的死板拦截。现已全面支持选取并应用 "所有文件 (*.*)"，玩家可将本地的游戏地图、文本文档、攻略记录均视作执行目标加以管理或挂载到并行启动项。