本次更新聚焦启动性能优化，并修复多项数据一致性问题。

---

## 🛠️ 优化（Improvements）

- **数据刷新缓存**：首次加载完成后，后续触发刷新（如切换存储模式、从设置页返回）不再重新读取配置文件，直接使用内存缓存数据
- **系统语言缓存**：系统语言检测结果在首次判断后缓存，避免重复查询操作系统区域设置
- **平台检测防抖**：在可执行文件路径输入框中键入时，增加 300ms 延迟再触发平台识别；快速连续输入时自动取消前一次检测请求，避免无效等待和竞态
- **公告列表去重**：公告内容 ID 组合未变化时跳过列表重建，避免不必要的 UI 刷新
- **工具提示隐藏**：已处于隐藏状态时提前返回，跳过后续所有清理操作
- **回收站自动清理**：直接在内存集合上操作而非重新读取文件，减少磁盘 I/O
- **图标大小调节**：初始化恢复用户设置值时抑制滑块事件触发，避免启动阶段多余的布局计算
- **Shell 原生图标提取**：新增从可执行文件中提取高分辨率原生图标的能力，图标清晰度优于旧方案
- **设置文件自动补全**：启动时自动检测设置文件是否缺少新增配置项，若缺失则自动补全默认值，无需手动编辑

---

## 🐛 缺陷修复（Bug Fixes）

- 修复拖拽排序后保存成功但下次启动或刷新时排序丢失的问题
- 修复扫描导入新游戏后列表未立即显示新项的问题
- 修复回收站自动清理过期项后内存集合未同步移除的问题
- 修复启动时图标大小滑块在恢复用户设置值过程中意外触发多次变更事件的问题
- 修复应用图标在 Windows 任务栏中显示异常的问题

---

## 🗂️ 内部调整（Internal）

- 应用图标文件从嵌入资源改为输出目录内容文件，便于自定义图标功能读取
- I18n 翻译查词从每次调用记录日志改为界面刷新后批量摘要输出
- 图标转换器（ImagePathConverter / NullToVisibilityConverter / BoolToVisibilityConverter）从逐次记录改为批量摘要
- 游戏项从配置文件加载时抑制逐属性变更日志，改为初始化完成后输出单条汇总
- 属性变更日志仅记录非初始值的后续变更，跳过默认值首次赋值
- 移除 NormalizeVersion、自定义提示框隐藏、图标容器尺寸应用中的冗余日志调用

---

## ⛳ 实验功能（Experimental）
`实验功能将不保证后续支持及维护！其随时可能被合并或移除，不作为正式功能提供！`
- **自定义应用标题与图标**：
  - 可在设置文件中通过 `appTitle` 字段自定义窗口标题栏与任务栏显示的名称，未设置时默认显示 "EricGameLauncher"
  - 可在设置文件中通过 `appIconPath` 字段指定自定义程序左上角及任务栏图标，未设置时自动回退到程序内置图标
  - 目前暂不提供设置UI，进阶用户需自行修改配置文件，配置文件修改后果自行承担
