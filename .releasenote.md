本次更新聚焦文件选择体验与多语言一致性，并且修复了bug。

### 🛠️ 通用优化 (Optimizations)
- **文件选择预设筛选**：
  - 默认提供“可执行文件 / 图像文件 / 所有文件”三类预设，并保留可切换筛选列表。
- **文件对话框本地化**：
  - 文件选择与图标选择对话框标题、筛选名称已接入多语言。
  - 可执行文件筛选去除不具独立入口的扩展名。
- **界面一致性**：
  - 清理多处硬编码文案，统一由 i18n 负责加载，减少语言切换时的闪烁与不一致。
- **技术细节**：
  - 调用方法合并代码引用，代码更简洁。
  
### 🐛 缺陷修复 (Bug Fixes)
- 修复扫描功能在删除失效项目时会把所有项目带走的 Bug。

### 🚀 发布流程优化 (Release Process)
- 现在 Pre-Release (开发版) 发布后，如果 48 小时左右内无 Bug 反馈，将自动转为 Latest 正式版。