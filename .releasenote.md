本次更新为大版本升级，聚焦配置文件架构重组、底层重写、高频操作响应速度与更新体验。
> [!IMPORTANT]
> 本次为大架构升级，1.0.x版本将不会强制升级到1.1.x版本。
> 1.0.x版本将不再具有服务器配置拉取功能，这意味着所有基于服务器配置的功能将失效。但你仍然可以通过内建的更新功能更新到最新发行版。
> 1.0.x版本配置在升级至此版本后不兼容，需要重新配置。
> 你可以在公告中按照指示回退至1.0.20.1，但这只是暂时的，1.0.x版本将不会接收到新功能支持和bug修复。
> 此外，1.0.20.1 版本存在多项严重缺陷（UWP 图标模糊或颠倒、扫描导入后无法显示新项、已安装应用被误判失效等），不推荐继续使用。即使需要重新配置，我们仍强烈建议更新至最新版本。

### ✨ 新功能（Features）
- **公告中心**：
	- 顶栏新增公告入口，可直接查看公告列表
	- 支持展开阅读公告正文
	- 公告正文中的链接可点击跳转
	- 未读提示与置顶/置底标识可视化呈现

### 🛠️ 优化（Improvements）
- **公告同步与展示**：
	- 启动时自动拉取云端公告，并按时间与置顶/置底顺序展示
	- 公告已读状态本地保存，跨次启动保持阅读状态
- **多语言策略调整**：
	- 移除繁体中文与日语界面支持，原有繁体/日语用户将回退至英语
	- 更新通道「稳定版」更名为「正式版」，说明文案同步精简
- **更新组件能力**：
	- 更新组件支持执行批处理或命令任务，便于复杂更新流程
- **数据变更响应**：
	- 优化项目属性（图标、标题、路径等）变更后界面自动刷新的稳定性
- **UWP 应用检测**：
	- 改进已安装 UWP 应用的识别方式，减少误判
- **更新进度回传**：
	- 更新进度可回传至主界面，便于在启动器内直接掌握更新状态
- **GitHub API 认证与容错**：
	- 设置面板新增 GitHub 令牌配置入口，支持输入、保存与编辑
	- 配置 Token 后，更新检查与公告获取的 API 限额从 60 次/小时提升至 5000 次/小时
	- 令牌仅在本地存储，不上传至任何第三方
	- 更新检查在配置 Token 后自动使用认证请求，享受更高 API 限额
	- Token 无效（401）时自动清除并回退至未认证模式，不影响正常更新检查
- **多语言条目整理**：
	- i18n 条目按字母序重排，便于检索与维护
- **图标提取策略**：
	- UWP 图标提取改为多尺寸降级尝试，优先高分辨率，提高清晰度与成功率
- **内存写回缓存**：
	- 所有高频操作（拖动图标大小滑块、调整窗口位置、切换设置开关等）改为纯内存读写
	- 数据仅在应用正常关闭或存储模式切换时一次性写入磁盘
	- 彻底消除滑块拖动等场景的频繁 IO，显著提升界面流畅度
- **配置文件拆分**：
	- 配置文件由单一的 `config.yaml` 拆分为 `settings.yaml`（用户设置）和 `items.yaml`（游戏列表与回收站）
	- 修改设置时仅需序列化轻量的 `settings.yaml`，不再读写包含全部游戏的大文件
	- `settings.yaml` 不设版本控制，增删字段零负担；`items.yaml` 保留版本控制与迁移保护
- **语言自动跟随系统**：
	- 移除设置面板上的语言选择控件，界面语言自动跟随操作系统
	- 减少一个用户需手动管理的设置项

### 🐛 缺陷修复（Bug Fixes）
- 修复 UWP 应用图标提取错误的问题
- 修复扫描导入后未及时刷新的问题
- 修复已安装的 UWP 应用在扫描器中被误判为已失效的问题
- 修复部分图标显示模糊的问题

### 💥 破坏性变更（Breaking Changes）
- **全局配置格式由 JSON 迁移为 YAML**：
	- 旧版本配置不兼容，需要重新配置
	- YAML 更适合多行文本与长内容，公告与说明类配置更易维护
	- YAML 对层级结构更直观，配置编辑可读性更高
	- YAML 更适合手工调整，减少转义与格式错误

### 📢 重要公告（Notice）
在更新到1.1.0版本后，配置文件格式发生了重大变更。请注意以下事项：
      旧版本的配置文件将不再兼容。
      由于前期调研和本地我们看到的用户规模显示，目前用户规模较小，此时进行一次破坏性更新，虽然会给部分用户带来不便，但从长远来看是必要的，以便我们能够引入更好的设计和功能。
      1.0.x版本将不再具有服务器配置拉取功能，这意味着所有基于服务器配置的功能将失效。
      由于用户环境中配置文件不尽相同，我们无法提供自动迁移工具。请您更新此版本后，手动重新配置您的设置。
      你可以在github中的历史release中下载1.0.20.1版本的安装包，以便继续使用旧版本，直到你准备好迁移到新版本。但请注意，旧版本将不再接收更新和支持，且该版本存在多项严重缺陷（UWP 图标模糊或颠倒、扫描导入后无法显示新项、已安装应用被误判失效等），不推荐继续使用。即使需要重新配置，我们仍强烈建议更新至最新版本。
      感谢您的理解和支持！
      1.0.20.1版本下载：https://github.com/EricZhang233/EricGameLauncher/releases/tag/v1.0.20.1
      下载后，如果你之前的配置路径为系统模式，则解压至任意位置后，直接运行即可；
      如果之前的配置路径为便携模式，则需要将原有的data文件夹复制到1.0.20.1版本的安装目录下，或直接将1.0.20.1的程序包覆盖解压至已更新新版本的目录下，以保留原有配置。
      我们不处理非最新版本的用户反馈和问题，请尽快迁移到新版本以获得更好的体验。

---

## 🔧 技术变更明细（Technical Changelog）

> 面向开发者的完整代码级变更记录，覆盖 v1.0.20.1 → v1.1.0 所有 diff。按模块拆分，包含设计动机与实现细节。该部分由AI总结，请以实际代码为准！

---

### 1. 序列化框架迁移：`System.Text.Json` → `YamlDotNet` v12.0.2

**动机**：YAML 对多行文本、层级嵌套、手工编辑的友好度远超 JSON。公告正文、多语言条目等长文本在 JSON 中需要转义换行符，可读性极差；同时 YAML 的无括号语法降低用户手工修改配置文件时的格式错误概率。

**变更范围**：

| 层级 | 变更 |
|------|------|
| NuGet 依赖 | 主项目、`updater.main`、`updater.cfgver` 三个 `.csproj` 均新增 `YamlDotNet` v12.0.2 |
| 序列化注解 | 全局 `[JsonPropertyName("key")]` → `[YamlMember(Alias = "key")]`；`[JsonIgnore]` → `[YamlIgnore]` |
| 反序列化 API | `JsonSerializer.Deserialize<T>(string)` → `new DeserializerBuilder().WithNamingConvention(CamelCaseNamingConvention.Instance).Build().Deserialize<T>(string)` |
| null 处理 | `[JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]` 全部移除——YamlDotNet 默认不序列化 `null` 值，行为等价 |
| `JsonConverter<int[]>` | `IntArrayJsonConverter` 删除——原用于将 `WindowBounds` 数组序列化为 JSON 数组，YAML 原生支持序列 |
| 嵌入资源 | `i18n.json` → `i18n.yaml`；`migration_rules.json` → `migration_rules.yaml` |
| csproj 通配符 | `updater.main` 和 `updater.cfgver` 的 `EmbeddedResource` 从逐个枚举文件名改为 `*.*` + MSBuild 元数据 `%(Filename)%(Extension)`，新增依赖文件时无需修改 csproj |

**命名约定**：统一使用 `CamelCaseNamingConvention.Instance`，YAML key 全部小写驼峰（`launchMode`、`closeAfterLaunch` 等），与 JSON 时期一致。

---

### 2. 配置模型 (`Core/Config.cs`)

**窗口位置/尺寸结构化**：

`AppSettings.WindowBounds`（`int[]`）→ `AppSettings.Window`（`WindowBoundsInfo`，含 `X/Y/Width/Height` 四个整型字段）。YAML 输出由 `[x, y, w, h]` 数组变为具名字段：

```yaml
window:
  x: -1
  y: -1
  width: 950
  height: 650
```

可读性与可手工编辑性显著提升，不再依赖数组索引约定。

**公告已读状态持久化**：

- `ServerConfigManager` 新增静态字段 `_readIds: HashSet<string>`（`OrdinalIgnoreCase`）
- `LoadReadIds()`：启动时从 `{SystemCachePath}/announcements.read` 读取，每行一个 ID
- `SaveReadIds()`：写入同一文件
- `IsRead(string id)` / `MarkAsRead(string id, bool notify)`：O(1) 查找与标记
- `MarkAsRead` 标记后触发 `AnnouncementsUpdated` 事件，驱动 UI 刷新
- 事件在保存后引发，`try/catch` 包裹防止订阅者异常影响持久化

**服务端配置扩展**：

`ServerConfigInfo` 新增 `List<Announcement>? Announcements` 字段，通过 `ServerConfigManager` 统一拉取、缓存、分发。原有 `ForceUpdate` 字段不变。

---

### 3. 公告系统 (`Core/Announcement.cs`，新文件，209 行)

**数据模型 (`Announcement`)**：

```
Id          — 唯一标识，用于已读状态追踪
Title_Zh    — 简体中文标题（Cn 为主，Zh 为备用）
Title_En    — 英文标题（非中文环境的首选）
Body_Zh     — 正文（同上回退逻辑）
Body_En     — 正文（同上回退逻辑）
Time        — ISO 8601 时间字符串
Position    — "top" | "bottom" | "normal"
Visible     — 云端可见性开关
```

**多语言回退算法** (`GetDisplayTitle` / `GetDisplayBody`)：

- `zh-CN` 环境：`FirstNonWhiteSpace(TitleCn, TitleZh)` → 再回退 `TitleEn`
- 其他环境：`FirstNonWhiteSpace(TitleEn)` → 再回退中文
- `FirstNonWhiteSpace` 实现为 `params string?[]` 逐一遍历

**排序优先级** (`GetPositionPriority`)：

| position | priority | 排序位置 |
|----------|----------|----------|
| `"top"` | 0 | 最前 |
| `"normal"` | 1 | 中间（按时间降序） |
| `"bottom"` | 2 | 最后 |

**视图模型 (`AnnouncementListItem`)**：

- 实现 `INotifyPropertyChanged`，支持 UI 绑定
- `IsRead` 变更时联动通知 `IsUnread`、`ReadStatusText`、`TitleWeight`
- `IsExpanded`：公告详情展开/折叠状态
- `HasMarker` / `MarkerGlyph`：置顶显示 `` (Segoe Fluent Icons)，置底显示 `↧` (U+21A7)
- `FromAnnouncement()` 工厂方法：合并 `ServerConfigManager.IsRead()` 状态，调用 `FormatTime()` 本地化时间

---

### 4. 国际化 (`Core/I18n.cs`)

**反序列化**：`JsonSerializer` → `DeserializerBuilder().WithNamingConvention(CamelCaseNamingConvention.Instance).Build()`

`CamelCaseNamingConvention` 会自动将 YAML key `langName` 映射到 C# 属性 `LangName`，无需额外注解。

**语言检测简化**：

繁体中文 (Zh-TW) 和日语 (JA) 的 `i18n.yaml` 条目已被移除，回退至英文。减少未维护翻译的语言露出。

---

### 5. 项目数据模型 (`Core/Item.cs`)

#### 5.1 文件合并

`Core/Run.cs`（70 行）整体合并入 `Item.cs`，包含三个运行模型类：

- `RunActionBase`：`INotifyPropertyChanged` 基类，含 `Path`、`IsAdmin` 属性及变更日志
- `AlternativeRunAction : RunActionBase`：替代启动，新增 `Enabled` 开关
- `AlongsideRunAction : RunActionBase`：伴随启动，新增 `Enabled` 开关

这些类与 `AppItem` 强耦合，合并避免循环引用风险并减少文件碎片。

#### 5.2 DTO 层级重构（破坏性 YAML 结构变更）

v1.0.20.1 的四个 Action DTO（`MainAction`、`ManagerAction`、`AltAction`、`AlongsideAction`）在序列化时平铺于配置根层级。v1.1.0 新增 `ActionsDto` 聚合类，统一嵌套在 `actions` 节点下：

```yaml
actions:
  main:      { path: "...", admin: false }
  manager:   { path: "...", admin: false }
  alt:       { enabled: false, command: "...", admin: false }
  alongside: { enabled: false, command: "...", admin: false }
```

YAML 层级更清晰，且为后续扩展其他 Action 类型预留空间。

**字段重命名**：

| 旧名 | 新名 | 位置 |
|------|------|------|
| `IconPath` | `Icon` | `AppItemDto` |
| `CustomMenu` (string) | `CustomMenu` (List\<CustomMenuItem\>) | `AppItemDto` |
| `AltAction.Path` | `AltAction.Command` | `AltActionDto` |
| `AlongsideAction.Path` | `AlongsideAction.Command` | `AlongActionDto` |
| `RecycleBinItems` | `recycleBin` | `ConfigData` YAML 别名 |
| `IsAdmin` | `admin` (YAML key) | 所有 DTO |

**自定义右键菜单格式变更**：

v1.0.20.1 将菜单序列化为单一字符串：`"Title1|cmd1|false:Title2|cmd2|true"`，使用 `:` 和 `|` 分隔，需 `Uri.EscapeDataString`/`UnescapeDataString` 编解码。

v1.1.0 改为 `List<CustomMenuItem>` 直接序列化：

```yaml
customMenu:
  - title: "打开官网"
    command: "https://..."
    admin: false
  - title: "配置工具"
    command: "config.exe"
    admin: true
```

移除了 `CustomMenu` 字符串属性的 getter/setter 和 `GetCustomMenuItems()` / `SetCustomMenuItems()` 中的编解码逻辑。

#### 5.3 内容指纹与变更检测

`AppItem` 新增 `GetContentFingerprint()` 方法：通过 `YamlDotNet.SerializerBuilder` 将 ViewModel 序列化为字符串指纹。

`CheckForDataChanges()` 重构：

- v1.0.20.1：仅比对 `Id` 集合是否 SetEquals——无法检测属性变更
- v1.1.0：逐项比对 `GetContentFingerprint()` 字符串，精确检测标题、图标、路径等任何属性变化
- 同步覆盖 `_allItems` 和 `_recycleItems` 两个集合
- 变更检测触发 `ConfigService.DataChanged` → UI 自动刷新（无需手动重启）

#### 5.4 `[YamlIgnore]` 注解标准化

`AppItem` 中 14 个 `[JsonIgnore]` 全部替换为 `[YamlIgnore]`，涵盖：

- 计算属性：`TitlePinyin`、`TitlePinyinInitial`、`TitleEnglishInitial`、`PlatformName`、`RuntimeManagerPath`、`HasManager`、`HasManagerOrDefault`、`IsPlatformUrl`、`DisplayIcon`
- 兼容性访问器：`ExePath`、`IsAdmin`、`MgrPath`、`IsMgrAdmin`、`UseAlternativeLaunch`、`AlternativeLaunchCommand`、`IsAltAdmin`、`RunAlongside`、`AlongsideCommand`、`IsAlongsideAdmin`

#### 5.5 属性变更通知精简

`AppItem` 中：
- `Status` setter：移除冗余的 `OnPropertyChanged(nameof(Status))`（`SetProperty` 已自动触发）
- `DeletedAt` setter：移除冗余的 `OnPropertyChanged(nameof(DeletedAt))` 和 `OnPropertyChanged(nameof(TimeBadgeVisibility))`
- `SortOrder` setter：移除冗余的 `OnPropertyChanged(nameof(SortOrder))`

#### 5.6 UWP 图标提取修复

**文件存在性校验**：

`GetIconPathAsync()` 和 `ExtractAndSaveIconAsync()` 新增早期返回：
```csharp
if (!exePath.StartsWith("shell:AppsFolder\\", ...) && !File.Exists(exePath))
    return null;
```
`shell:AppsFolder\` 为虚拟路径，`File.Exists()` 对其始终返回 false，需特殊处理。

**多尺寸降级提取**：

v1.0.20.1 仅请求 256px，使用非标准 flags `0x104`/`0x108`。v1.1.0 改为按优先级降级尝试 10 个尺寸 `[512, 256, 150, 128, 96, 72, 64, 48, 44, 32]`，flags 修正为 `0x4`（`IISIF_ASPECTRATIO`）和 `0x8`（`IISIF_ASPECTRATIO | IISIF_LARGER`），符合 MSDN 文档规范。高分辨率优先，首成功即返回，修复了部分 UWP 应用返回低分辨率或颠倒图像的问题。

---

### 6. 平台扫描 (`Core/PlatformGameCore.cs`)

**Steam VDF 解析日志瘦身**（性能优化）：

| 方法 | 移除的日志调用数 | 移除的 `StartOperation` 包装 |
|------|:--:|:--:|
| `ExtractAppIdFromUrl()` | 2 | — |
| `DetectSteamPath()` | 3 | — |
| `GetLibraryFolders()` | 3 | 1 |
| `GetGameInfo()` | 3 | 1 |
| `GetExecutableFromSteamUrl()` | 2 | — |
| `FindMainExecutable()` | 2 | 1 |

Steam 扫描路径为高频调用链（每次刷新 / 导入均触发），每处 `LogService.Write` 涉及锁竞争与 I/O，累积开销不可忽略。

**VDF 解析稳健性提升**：`GetLibraryFolders()` 在 `libraryfolders.vdf` 不存在时提前返回缓存值，避免 `FileNotFoundException` 进入异常路径。

**正则优化**：移除 3 处 `RegexOptions.IgnoreCase`——Steam VDF 的 key 始终为小写（`"path"`、`"installdir"`、`"name"`），无需大小写不敏感匹配。

**新增 `ReadCString()` 辅助方法**：从 `BinaryReader` 读取以 `\0` 结尾的 UTF-8 C 字符串（最大 4096 字节），为二进制 VDF 解析预留。

**新增 using**：`System.Runtime.InteropServices`、`System.Xml`（为 UWP AppxManifest XML 解析预留）。代码风格统一：数组初始化改用 C# 12 集合表达式；注册表路径逐字字符串移除多余双反斜杠转义。

---

### 7. 更新服务 (`Core/Updater.cs`)

**HttpClient 生命周期管理**：

v1.0.20.1 每次请求前检查并设置 `User-Agent` / `Accept` 头——违反 `HttpClient` 最佳实践（请求头应在构造时设置，避免每次请求的字典查找开销）。v1.1.0 改为 `static constructor` 集中初始化。

**GitHub API 认证支持**：新增 `ApplyGitHubToken()` 方法，debug.yaml 传入 Personal Access Token，添加 `Authorization: Bearer` 请求头。未认证 API 限额为 60 req/h，认证后提升至 5000 req/h。

**方法签名变更**：`StartUpdater(string)` → `StartUpdaterAndWaitAsync(string, Action<string>?)`，从 fire-and-forget 改为异步等待 + 进度回调。

**资源提取泛化**：从硬编码 3 个资源文件名改为运行时反射遍历 `assembly.GetManifestResourceNames()`，按前缀 `EricGameLauncher.updater.main.` 过滤。当 updater.main 新增依赖（如 YamlDotNet.dll）时，无需修改此处代码。

**NamedPipe 进程间通信**：

```
协议（文本行，由更新器 → 主进程）：
  DOWNLOAD 0           — 下载开始
  DOWNLOAD 45.2        — 下载进度百分比
  DOWNLOAD 100.0       — 下载完成
  READY                 — 准备替换文件（终止信号）

服务端配置：
  PipeDirection.In
  PipeTransmissionMode.Byte
  PipeOptions.Asynchronous
  CancellationTokenSource(TimeSpan.FromMinutes(5))
```

`onProgress` 回调每行触发一次，主进程可借此更新 UI 进度条或状态文本。

**进程退出等待**：

更新器参数新增 `--main-pid {pid}`。主进程启动更新器后阻塞在 `pipeServer.WaitForConnectionAsync()`，收到 `READY` 后由更新器等待主进程退出，避免文件替换时的锁定冲突。

---

### 8. 更新组件 (`Updater/updater.main/Program.cs`)

**日志独立性**：

v1.0.20.1 依赖主进程的 `LogService.Write`（通过源码链接共享），存在跨进程文件锁竞争。v1.1.0 改为自包含文件日志，路径为 `%TEMP%\eric\ericgamelauncher\log\updater.main.{yyyyMMdd-HHmmss}.log`，不再依赖任何外部类型。

**任务文件执行 (`TryRunTaskFile`)**：

更新器启动时检查同目录下 `updater.main.tasks.yaml`，若存在则解析并执行后退出，跳过正常更新流程。

YAML 格式：
```yaml
- Type: bat          # bat | cmd
  Path: "setup.bat"  # 仅 bat 需要
  Args: "/S"         # 可选，bat 参数
  Command: "echo done"  # 仅 cmd 需要
  WorkDir: "C:\\temp"   # 可选，工作目录
  Wait: true             # 可选，是否等待完成
```

`NormalizeYamlObject()` 递归转换 YamlDotNet 反序列化产生的 `Dictionary<object, object>` 为 `Dictionary<string, object>`，统一类型便于后续操作。

**CLI 独立模式**：

```
updater.main.exe --run-bat <path> [--args <args>] [--workdir <dir>] [--wait]
updater.main.exe --run-cmd <command> [--workdir <dir>] [--wait]
```

`TryHandleCommandMode()` 解析 `--run-bat` / `--run-cmd` 前缀，使更新器可作为独立脚本执行器使用，无需嵌入主进程。

**进程管理**：

`StartProcess()` 封装 `Process.Start()`：
- `UseShellExecute = false` + `CreateNoWindow = true`：无窗口静默执行
- `WorkingDirectory`：可指定，默认当前目录
- `wait` 参数控制是否 `WaitForExit()` 并记录退出码

**NamedPipe 客户端**：`NamedPipeClientStream` 连接主进程管道（5s 超时），`StreamWriter` + `AutoFlush` 实时发送。下载循环中每 200ms 或每 1MB 上报一次 `DOWNLOAD {percent:F1}`；下载完成后发送 `READY`，然后等待主进程退出。

**流程时序**：

```
Main Process                    Updater
    |                              |
    |-- Start Process ----------->|
    |                              |-- [可选] TryRunTaskFile()
    |                              |-- 下载 zip
    |   <-- DOWNLOAD 0 ------------|
    |   <-- DOWNLOAD 45.2 ---------|
    |   <-- DOWNLOAD 100.0 --------|
    |   <-- READY -----------------|
    |                              |-- 等待 Main Process 退出
    |-- 收到 READY, 开始退出       |
    |-- 进程终止                   |
    |                              |-- 关闭残留 EricGameLauncher 进程
    |                              |-- 解压并替换文件
    |                              |-- 重启 EricGameLauncher
```

---

### 9. 配置版本更新器 (`Updater/updater.cfgver/`)

- `migration_rules.json` → `migration_rules.yaml`：YAML 内容为空列表 `[]`（v1.0→v1.1 无自动迁移规则）
- `updater.cfgver.csproj`：`EmbeddedResource` 从 `migration_rules.json` 改为 `migration_rules.yaml`
- 新增 `YamlDotNet` v12.0.2 依赖
- `.csproj` 嵌入资源同样改为 `*.*` 通配符

---

### 10. UI 转换器重构 (`Core/Converters.cs`，新文件)

**拆分来源**：`WinShell.cs` 中混杂了 Win32 Shell 操作（`ShortcutResolver`、`ShortcutScanner`）与 XAML 转换器，职责不清。

**保留的转换器**（4 个，从 WinShell.cs 迁移）：

| 转换器 | 用途 |
|--------|------|
| `ImagePathConverter` | 文件路径 → `BitmapImage`，含 `WeakReference` 缓存 |
| `NullToVisibilityConverter` | `null` → `Visible`；非 null → `Collapsed` |
| `BoolToVisibilityConverter` | `true` → `Visible`；`false` → `Collapsed` |
| `SizeToCornerRadiusConverter` | `double` 尺寸 × `Ratio` → `CornerRadius` |

**`ImagePathConverter` 增强**：新增 `BitmapCreateOptions.IgnoreImageCache`，配合 URI 中附加的 `?t={LastWriteTime.Ticks}` 查询参数，图标文件更新后强制重新加载，避免 `BitmapImage` 默认缓存导致显示旧图。

**废弃的转换器**（2 个）：

- `StringNotEmptyToVisibilityConverter`：`App.xaml` 中已无引用
- `BoolToOpacityConverter`：`App.xaml` 中已无引用

**`App.xaml` 资源声明同步清理**：移除上述 2 个转换器的 `<local:...Converter x:Key="..."/>` 声明。

---

### 11. `Core/WinShell.cs` 清理

移除 7 个类：
- 6 个转换器（移至 `Converters.cs` 或废弃）
- `IntArrayJsonConverter`（JSON→YAML 后不再需要）

移除 4 个 `using`：
- `System.Text.Json`
- `System.Text.Json.Serialization`
- `Microsoft.UI.Xaml`
- `Microsoft.UI.Xaml.Data`
- `Microsoft.UI.Xaml.Media.Imaging`

保留 4 个功能模块：`ShortcutInfo`、`ShortcutResolver`、`ShortcutScanner`（含 `FileItem`）、`Win32FileDialog`。

---

### 12. 主窗口 (`MainWindow.xaml` / `MainWindow.xaml.cs`)

#### 12.1 公告 Flyout UI (XAML)

顶栏新增 `AnnouncementButton`（`FontIcon Glyph=`）+ `Flyout`，关键设计点：
- `Placement="BottomEdgeAlignedRight"`：Flyout 在按钮下方展开
- `MaxHeight="420"`：限制列表高度，超出滚动，避免撑破屏幕
- `IsItemClickEnabled="True"`：整行可点击，非仅 CheckBox 区域
- 无内联 `ItemTemplate`——在 C# 后端通过 `ItemTemplateSelector` 或动态构建

#### 12.2 公告交互逻辑 (C#)

**数据刷新** (`RefreshAnnouncementList`):

1. 从 `ServerConfigManager.CurrentConfig?.Announcements` 获取公告列表
2. `Select(AnnouncementListItem.FromAnnouncement)` 转换为视图模型
3. `OrderBy(GetPositionPriority).ThenByDescending(GetTimeValue)`：置顶→普通→置底，同级按时间倒序
4. 空列表时显示 `AnnouncementsEmptyText`（`Visibility.Visible`），隐藏 `AnnouncementsListView`
5. 已读状态来自 `ServerConfigManager.IsRead(id)`，不依赖本地缓存

**展开/折叠**：

- 点击公告 → 切换 `IsExpanded`
- 同一时间仅允许一个公告展开（遍历其他 item 设置 `IsExpanded = false`）
- 展开时自动标记已读：`ServerConfigManager.MarkAsRead(item.Id, notify: false)` + `item.IsRead = true`

**Flyout 生命周期**：

- `Opened` 事件 → `RefreshAnnouncementList()`
- 关闭时 `ServerConfigManager.AnnouncementsUpdated` 事件可能触发刷新，重新打开获取最新数据

#### 12.3 变更检测重构 (`CheckForDataChanges`)

```
// v1.0.20.1: 仅检测 Id 集合变化（新增/删除），属性变更不触发刷新
var oldIds = new HashSet<string>(_allItems.Select(x => x.Id));
var newIds = new HashSet<string>(newItems.Select(x => x.Id));
if (!oldIds.SetEquals(newIds)) return true;

// v1.1.0: 逐项比对指纹，属性变更也触发刷新
var oldById = _allItems.ToDictionary(x => x.Id);
foreach (var newItem in newItems)
{
    if (!oldById.TryGetValue(newItem.Id, out var oldItem))
        return true;  // 新增
    if (oldItem.GetContentFingerprint() != newItem.GetContentFingerprint())
        return true;  // 属性变更
}
```

同步覆盖 `_recycleItems`。变更时 `ConfigService.DataChanged` 事件驱动 `DispatcherQueue.TryEnqueue(() => RefreshView())`。

#### 12.4 扫描导入修复

v1.0.20.1 的 bug：`ImportScannedGames()` 在 `SaveItems()` 之前将 `newItem` 加入 `_allItems`，然后对 UWP 应用异步提取图标，但未等图标提取完成就调用了 `SaveItems`——导致图标路径为空即保存。且 `SaveItems` 后未调用 `RefreshGlobalAsync`。

修复：
1. 不再将 newItems 逐个加入 `_allItems`
2. 使用 `Concat` 合并后一次性 `SaveItems`
3. `SaveItems` 后显式调用 `ConfigService.RefreshGlobalAsync()`，确保 `DataChanged` 触发 UI 刷新

#### 12.5 更新流程重构 (`ManualCheck`)

- 非强制更新且无更新时，显示 `Update_NoUpdateContent` 对话框
- 对话框 `FullSizeDesired = false`，避免全屏展开

#### 12.6 隐私窗口滚动修复

v1.0.20.1 的 `Privacy_DialogContent` 在 `ContentDialog` 中直接嵌入 `ScrollViewer`，但缺少 `VerticalScrollMode = Enabled` 和 `MaxHeight` 约束，长内容无法滚动。v1.1.0 显式设置 `MaxHeight = 400`、`VerticalScrollMode = Enabled`，并新增 `IsTextSelectionEnabled = true` 允许用户选择复制文本。

#### 12.7 启动优化

- `AppGrid.Focus(FocusState.Programmatic)`：2 处（早期显示 + 超时兜底），确保启动后键盘导航可用
- `StartupArgs.Parse()` + `DebugPaths.ApplyIfDebug()` + `ServerConfigManager.LoadReadIds()` 在构造函数中提前执行
- `ConfigService.DataChanged` 订阅提前至构造函数末尾
- `RefreshAnnouncementList()` 在 `LoadSettings()` 后立即调用

#### 12.8 资源提取泛化（cfgver）

与 `Updater.cs` 中 updater.main 资源提取逻辑一致，cfgver 嵌入资源提取也改为运行时反射遍历 `assembly.GetManifestResourceNames()`，按前缀 `EricGameLauncher.updater.cfgver.` 过滤。

#### 12.9 窗口关闭时取消事件订阅

```csharp
// MainWindow_Closed
ServerConfigManager.AnnouncementsUpdated -= OnAnnouncementsUpdated;
```

防止静态事件的委托引用阻止 GC 回收 MainWindow 实例。

---

### 13. 启动入口 (`App.xaml` / `App.xaml.cs`)

**`App.xaml`**：
- 移除 `StringNotEmptyToVisibilityConverter` 资源声明（1 行）
- 移除 `BoolToOpacityConverter` 资源声明（1 行）

**`App.xaml.cs`** 构造函数新增：
```csharp
try { StartupArgs.Parse(); DebugPaths.ApplyIfDebug(); } catch { }
```
在 `InitializeComponent()` 之前解析命令行参数，确保调试路径在 XAML 加载前生效。

---

### 14. 日志服务 (`Core/LogService.cs`)

**会话时间戳**：

```csharp
// v1.0.20.1
private const long MaxLogFileBytes = 5 * 1024 * 1024;
private static string LogFilePath => Path.Combine(LogDir, "app.log");

// v1.1.0
private static readonly string _sessionTimestamp =
    DateTime.Now.ToString("yyyyMMdd-HHmmss");
private static string LogFilePath =>
    Path.Combine(LogDir, $"app-{_sessionTimestamp}.log");
```

- `MaxLogFileBytes` 常量移除——不再按大小截断，改用按会话分文件
- 每次进程启动生成独立日志文件，文件名含精确到秒的时间戳
- `_sessionTimestamp` 为 `static readonly`，整个进程生命周期不变

---

### 15. 项目工程 (`EricGameLauncher.csproj`)

```xml
<!-- 新增依赖 -->
<PackageReference Include="YamlDotNet" Version="12.0.2" />

<!-- 嵌入资源替换 -->
- <EmbeddedResource Include="i18n.json" />
+ <EmbeddedResource Include="i18n.yaml" />

<!-- updater.main 嵌入资源：显式枚举 → 通配符 -->
- <EmbeddedResource Include="payload\updater.main\updater.main.exe"
    LogicalName="EricGameLauncher.updater.main.exe" />
- <EmbeddedResource Include="payload\updater.main\updater.main.dll"
    LogicalName="EricGameLauncher.updater.main.dll" />
- <EmbeddedResource Include="payload\updater.main\updater.main.runtimeconfig.json"
    LogicalName="EricGameLauncher.updater.main.runtimeconfig.json" />
+ <EmbeddedResource Include="payload\updater.main\*.*"
    LogicalName="EricGameLauncher.updater.main.%(Filename)%(Extension)" />

<!-- updater.cfgver 嵌入资源：同上 -->
- <EmbeddedResource Include="payload\updater.cfgver\updater.cfgver.exe"
    LogicalName="EricGameLauncher.updater.cfgver.exe" />
- ... (2 more)
+ <EmbeddedResource Include="payload\updater.cfgver\*.*"
    LogicalName="EricGameLauncher.updater.cfgver.%(Filename)%(Extension)" />
```

MSBuild 通配符 `*.*` 自动匹配 `payload/` 子目录下所有文件，`%(Filename)%(Extension)` 元数据保留原始文件名。新增依赖（如 `YamlDotNet.dll`）时无需手动修改 csproj。

---

### 16. 其他文件变更

| 文件 | 变更 | 影响 |
|------|------|------|
| `Version.cs` | `"1.0.20.1"` → `"1.1.0"` | 版本标识 |
| `ServerCfg.yaml` | 新增 75 行 | 服务端配置模板，YAML 格式 |
| `.todo.md` | 删除 (17 行) | 开发任务清单，不再维护 |
| `output/EricGameLauncher_1.0.20.1.zip` | 删除 (13 MB) | 旧版构建产物清理 |
| `.github/copilot-instructions.md` | +10 行 | 新增守则 #5：Release Note 编写规范 |
| `.github/readme_res/readme_en.md` | 2 处 | `System.Text.Json` → `YamlDotNet`；缩进统一 |
| `.github/readme_res/readme_jp.md` | 2 处 | 同上 |
| `readme.md` | 2 处 | 同上 |
| `.releasenote.md` | 全文重写 | v1.1.0 发版说明 |

---

### 18. 配置文件拆分与内存写回缓存 (`Core/Config.cs`)

**数据模型拆分**：

`ConfigData`（含 `Version` + `Settings` + `Items` + `RecycleBinItems`）拆分为两个独立模型：

| 模型 | YAML 文件 | 内容 | 版本控制 |
|------|-----------|------|----------|
| `AppSettings` | `settings.yaml` | `launchMode`、`closeAfterLaunch`、`iconSize`、`updateChannel`、`window` | ❌ 无 |
| `ItemsData` | `items.yaml` | `version`、`items`、`recycleBin` | ✅ `CurrentConfigVersion` |

`ConfigData` 类整体删除，`ItemsData` 仅保留游戏数据相关字段。

**内部状态重构**：

```
_configData: ConfigData  →  _settings: AppSettings  +  _itemsData: ItemsData
_SaveSemaphore           →  删除（保存改为同步，不再需要信号量）
```

**保存策略变更**：

| 方法 | 改前 | 改后 |
|------|------|------|
| `SaveConfigData()` | fire-and-forget 异步，序列化整包 `ConfigData`，经 `YamlQuoteHelper` 转单引号 | 删除 |
| `SaveConfig()` | 调 `SaveConfigData()` | 删除 |
| `SaveAll()` | — | **新增**：同步顺序写入 `settings.yaml` + `items.yaml` |
| `SaveSettingsData()` | — | **新增**：`SerializerBuilder().Build().Serialize(_settings)` → `File.WriteAllText` |
| `SaveItemsData()` | — | **新增**：`SerializerBuilder().Build().Serialize(_itemsData)` → `File.WriteAllText` |
| `SaveItems()` | 调 `SaveConfig()` 写盘 | 仅更新内存，不写盘 |

**调用点变化**（7 处分散保存全部删除，改为 2 处集中保存）：

| 原位置 | 操作 |
|--------|------|
| `SaveWindowState` | 移除 `Task.Run(() => SaveConfig())` |
| `ToggleCloseAfterLaunch_Toggled` | 移除 `SaveConfig()` |
| `ComboLaunchMode_SelectionChanged` | 移除 `Task.Run(() => SaveConfig())` |
| `ComboUpdateChannel_SelectionChanged` | 移除 `SaveConfig()` |
| `SizeSlider_ValueChanged` | 移除 `SaveConfig()` — **关键优化点**，滑块每次 ValueChanged 不再触发 IO |
| `BtnSwitchStorageMode_Click` | 移除 `SaveConfig()`（`SwitchStorageMode` 内部已调 `SaveAll()`） |
| `LanguageComboBox_SelectionChanged` | 随控件一并删除 |
| `MainWindow_Closed` | **新增** `SaveAll()` — 正常关闭时全量保存 |
| `CloseAfterLaunchTimer_Tick` | **新增** `SaveAll()` — 启动后自动关闭前全量保存 |

**路径检测更新**：

`Initialize()` 中不再检查旧 `config.yaml`，改为同时检查 `settings.yaml` 或 `items.yaml`（任一存在即为对应模式）。

**`SwitchStorageMode` 适配**：存储模式切换前调 `SaveAll()`，复制文件时遍历 `{ SettingsFileName, ItemsFileName }` 两个文件。

**`YamlQuoteHelper` 删除**（~35 行）：序列化回归 YamlDotNet 默认双引号。`using System.Text.RegularExpressions` 同步移除。

**`ConfigService.Language` 属性简化**：

```csharp
public static string Language => I18n.DetectSystemLanguage();
```

`AppSettings.Language` 字段及 `[YamlMember(Alias = "language")]` 注解一并删除。

---

### 19. UI 语言选择控件移除 (`MainWindow.xaml`)

删除设置 Flyout 中的语言选择区域（`Grid` + `FontIcon` + `ComboBox x:Name="LanguageComboBox"`，~8 行 XAML）。

---

### 20. 主窗口保存与语言逻辑清理 (`MainWindow.xaml.cs`)

**删除**：
- `LanguageComboBox_SelectionChanged` 方法（~12 行）
- `ApplyLocalization()` 中 LanguageComboBox 填充代码（~11 行）
- 6 处分散的 `SaveConfig()` 调用（见 §18 调用点表）

**新增**：
- `MainWindow_Closed` → `ConfigService.SaveAll();`
- `CloseAfterLaunchTimer_Tick` → `ConfigService.SaveAll();`（在 `Application.Exit()` 前）

**修正**：
- cfgver 迁移路径：`ConfigService.ConfigFilePath` → `ConfigService.ItemsFilePath`

---

### 21. `updater.cfgver` JSON→YAML 重写 (`Updater/updater.cfgver/Program.cs`)

**问题**：`updater.cfgver.csproj` 已引用 `YamlDotNet v12.0.2`，但 `Program.cs` 仍使用 `System.Text.Json`（`JsonNode`/`JsonObject`/`JsonArray`）。嵌入资源名写死为 `migration_rules.json`，与实际 `.yaml` 资源不匹配。

**重写范围**：

| 组件 | 改前 | 改后 |
|------|------|------|
| using | `System.Text.Json`、`System.Text.Json.Nodes` | YamlDotNet `DeserializerBuilder`/`SerializerBuilder` |
| 规则文件加载 | `assembly.GetManifestResourceStream("...migration_rules.json")` | `"...migration_rules.yaml"` |
| 配置解析 | `JsonNode.Parse(configJson)?.AsObject()` | `deserializer.Deserialize<Dictionary<object, object>>(configYaml)` |
| 字典访问 | `jsonObj.TryGetPropertyValue("key", out var v)` + `v.GetValue<int>()` | `dict.TryGetValue("key", out var v)` + `Convert.ToInt32(v)` |
| 数组访问 | `jsonObj["key"]?.AsArray()` | `dict.TryGetValue("key", out var v) && v is List<object> arr` |
| 深拷贝 | `JsonNode.DeepClone()` | `DeepCopyYaml(obj)` — YAML 序列化往返 |
| 序列化输出 | `configRoot.ToJsonString(options)` | `serializer.Serialize(configRoot)` |
| 配置键名 | `"Version"` / `"recycleBinItems"`（PascalCase/旧 JSON 键） | `"version"` / `"recycleBin"`（camelCase/实际 YAML 键） |

**新增辅助方法 `DeepCopyYaml()`**：
```csharp
private static object DeepCopyYaml(object obj)
{
    var serializer = new SerializerBuilder().Build();
    var deserializer = new DeserializerBuilder().Build();
    return deserializer.Deserialize<object>(serializer.Serialize(obj));
}
```

通过 YAML 序列化往返实现深拷贝，避免手动递归处理嵌套字典/列表。

**使用说明文本更新**：`"Usage: updater.cfgver.exe <config_path>"` → `"<items_path>"`。

---

### 22. 启动日志适配 (`Core/StartupArgs.cs`)

`LogEnvironment()` 输出适配双文件结构：
```csharp
// 改前
$"Config={ConfigFilePath} (exists=...)"
// 改后
$"Settings={SettingsFilePath} (exists=...) Items={ItemsFilePath} (exists=...)"
```

---

### 23. GitHub Token 配置与更新认证 (`Core/Config.cs` / `Core/Updater.cs` / `Core/StartupArgs.cs` / `MainWindow.xaml` / `MainWindow.xaml.cs`)

**动机**：GitHub 未认证 API 限额为 60 req/h，用户频繁检查更新或公告拉取可能在短时间内耗尽配额。GitHub Personal Access Token 可将限额提升至 5000 req/h。

**数据模型变更 (`Config.cs`)**：

`AppSettings` 新增 `GitHubToken` 字段（`[YamlMember(Alias = "githubToken")]`），持久化于 `settings.yaml`：
```yaml
githubToken: ""
```

`ConfigService` 新增 `GitHubToken` 静态属性，getter 返回 `_settings?.GitHubToken ?? ""`，setter 写入内存并记录日志（Token 值本身不记入日志）。

**启动参数简化 (`StartupArgs.cs`)**：

- 移除 `GitHubToken` 静态属性——Token 不再从启动参数或 debug.yaml 注入，统一从 `ConfigService.GitHubToken` 读取
- 移除 `LoadDebugConfig()` 方法（~20 行）——该功能被设置面板 UI 取代
- `using System.Collections.Generic`、`YamlDotNet.Serialization`、`YamlDotNet.Serialization.NamingConventions` 同步移除

**更新认证重构 (`Updater.cs`)**：

`ApplyGitHubToken()` 方法重写：
- 从 `ConfigService.GitHubToken` 读取 Token（原从 `StartupArgs.GitHubToken`）
- 新增 `_tokenApplied` 标志位，防止重复添加请求头
- 无 Token 时记录 `"no token configured, falling back to unauthenticated (60 req/h)"`

`InvalidateToken()` 新增：
- 清空 `ConfigService.GitHubToken` 并调用 `ConfigService.SaveAll()` 持久化
- 移除 `Authorization` 请求头，重置 `_tokenApplied` 标志
- 日志记录 `"invalid token cleared, fallback to unauthenticated"`

`FetchReleasesWithFallbackAsync()` 新增（提取公共逻辑）：
```
ApplyGitHubToken() → 请求 → 成功返回
                    → 401 且已配置 Token → InvalidateToken() → 无认证重试 → 返回
                    → 401 且无 Token → 返回 null
                    → 其他异常 → 返回 null
```

`GetLatestReleaseAsync()` 和 `GetLatestStableReleaseAsync()` 调用方简化：直接调用 `FetchReleasesWithFallbackAsync()`，无需各自处理 Token 与异常。

**设置 UI (`MainWindow.xaml`)**：

设置 Flyout 中「更新通道」区域下方新增 GitHub Token 配置区块（~50 行 XAML）：
- `SettingsGitHubTokenLabel` + `FontIcon`：区块标题
- `GitHubTokenViewRow`（默认可见）：显示配置状态文本 + 编辑按钮
- `GitHubTokenEditRow`（默认隐藏）：`PasswordBox` 输入框 + 保存/取消按钮
- `SettingsGitHubTokenDesc`：说明文本
- `SettingsGitHubTokenLink`：跳转至 GitHub Token 创建页面的超链接

**设置交互逻辑 (`MainWindow.xaml.cs`)**：

| 方法 | 行为 |
|------|------|
| `MenuSettings_Click` | 打开 Flyout 时调用 `UpdateGitHubTokenStatus()`，切换到 View 模式 |
| `GitHubTokenEditBtn_Click` | 切换到 Edit 模式，填充当前 Token 值，聚焦输入框 |
| `GitHubTokenSaveBtn_Click` | 比较新旧值，有变化时写入 `ConfigService` 并 `SaveAll()`，切换回 View 模式 |
| `GitHubTokenCancelBtn_Click` | 放弃编辑，切换回 View 模式 |
| `SwitchToTokenView()` | 隐藏编辑行，显示查看行，刷新状态文本 |
| `UpdateGitHubTokenStatus()` | 根据 `ConfigService.GitHubToken` 是否为空显示「已配置」/「未配置」 |
| `ApplyLocalization()` | 为 8 个新增 UI 元素绑定 i18n 文本 |

**初始化流程变更**：

`MainWindow` 构造函数中移除 `StartupArgs.LoadDebugConfig()` 调用——Token 现在从 `settings.yaml` 加载，不再需要 debug.yaml 注入。

---

### 24. i18n 条目字母序重排与 Token 翻译补全 (`i18n.yaml`)

**重排**：Zh-CN 与 EN 区块的 key 按字母序（A→Z）重新排列，消除历史累积的无序状态，便于人工检索与 diff 对比。所有现有翻译值保持不变。
