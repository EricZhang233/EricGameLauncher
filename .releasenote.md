本次更新聚焦启动性能与稳定性改进，并修复若干启动与图标相关问题。
*此版本为重要改进，已设置为**受支持的最低版本***
### 🛠️ 优化（Improvements）
- **启动后自动关闭**：当启用“启动后关闭”时，新增 15 秒延迟自动退出机制；在检测到窗口仍为前台或有用户鼠标/键盘活动时会取消退出，避免意外关闭。
- **启动渲染稳定性**：重写了启动渲染逻辑，重排了启动事件，提升了稳定性。
- **设置界面路径图标区分**：修正设置界面中“打开配置”和“打开缓存”两个路径按钮使用相同图标的问题。
- **图标选择器（文件筛选）**：在“更改图标”对话中新增两类筛选。默认“可执行 + 图像”，“所有文件”备选。
- **日志系统**：日志系统全面覆盖，为后续debug提供支持。
- **启动性能**：实现智能 UI 准备检测，显著提升启动体验。
- **增量数据更新**：新增仅在数据实际改变时重建集合，避免不必要的 UI 刷新与 GC 压力。
- **延迟加载复杂控件**：将菜单初始化与图标资源的加载延迟，将非关键工作移出启动关键路径。
- **网络检查异步化**：将更新检查移动到后台，不再阻塞启动流程，改善首屏响应。
- **RefreshView 优化**：通过增量检测与按需重建，减少了不必要的 RefreshView 重建，缓解了启动及运行时的短时耗时峰值。

### 🐛 缺陷修复（Bug Fixes）
- **属性面板 — 更改图标异常**：修复在属性面板通过“更改图标”选择图片时，返回系统缩略图或不正确图像的问题。
- **更多选单 — UI**：更多选单内启动器logo消失问题已修复。
- **设置界面 — 语言设置**：修复了设置界面内语言设置显示与实际应用不一致的问题。
- **设置界面 — 存储设置**：修复了迁移配置按钮未按预期工作的问题。

### 💥 破坏性变更（Breaking Changes）
- **i18n 精简**：为降低维护成本，本版本起将仅保留简体中文（Zh-CN）、英文（EN）、繁体中文（Zh-TW）和日文（JA）的语言支持，其他语言将被从程序内删除。