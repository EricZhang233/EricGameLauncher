本次更新带来了全新的命令行界面，完成了底层架构的全量 Core 化重构，并优化了单实例运行体验，为后续功能扩展奠定基础。

---

## ✨ 新功能（Features）

- **命令行界面（CLI）**：新增全功能命令行工具 `EricGameLauncher.Cli.exe`
  支持 `--json` 输出格式，方便脚本与自动化集成；通过 `-help` 可查看完整帮助信息
- **安装脚本**：输出目录新增 `.install.cmd`，便于通过命令行一键安装启动器到开始菜单和桌面
- **AI Agent 接入**：
  - 发布包新增 `SKILL.md` 与内置接入文档，AI Agent运行 `EricGameLauncher.Cli.exe skill` 即可获取完整使用指南

## 🛠️ 优化（Improvements）

- **单实例运行**：
  - 重复启动程序时不再打开多个窗口，而是自动聚焦到已运行的窗口
- **系统版本提示**：
  - 在不支持的 Windows 版本上启动时，弹出明确提示说明最低系统要求，而非无提示退出


## 🐛 缺陷修复（Bug Fixes）

- 修复带参数启动时，含空格的路径参数被错误拆分、导致程序无法正确启动的问题
- 修复修改数据后未立即写入磁盘、程序异常退出时可能丢失修改的问题
- 修复配置文件版本迁移失败时反复重启卡死、且无任何记录的问题：迁移失败会明确记录，并自动保留旧配置、以全新配置正常启动

## 🗂️ 内部调整（Internal）

- 游戏项状态归一化、数据变更检测、启动控制、扫描导入等核心业务逻辑全部从界面层抽离至 Core 层独立服务，GUI 与 CLI 共享同一套代码，界面代码瘦身约 1200 行
- 更新检测中的版本号解析逻辑统一收口，消除多处重复实现
- 国际化模块由 `I18n` 更名为 `Text`，配置文件由 `i18n.yaml` 更名为 `text.yaml`
- 更新器组件与 CLI 组件统一归入 `Components/` 目录管理
- 项目开发规则从 `.github/copilot-instructions.md` 迁移至 `agent.md`
- 构建流程新增 CLI 编译目标，构建后自动将 CLI 产物与说明文档复制至主程序输出目录
- 配置迁移工具改用独立日志文件记录，迁移失败时以错误级别明确写入日志
