# Eric Game Launcher (EGL) **v1.0.13** - 跨版本日志堆叠追踪，持续打磨细节

引入跨版本更新的发行日志自动合并叠加展示机制，不再错过任何改动与特性。同时继续优化多项 UI 交互细节，包含搜索浮窗的防误触收起、向左自然展开动画以及更纯粹的原生深浅色主题自适应体验。

> [!Important]
> 今后的发行标题与日志正文中将不再通过文字显式区分通道。区分开发/稳定版的唯一标准将统一依赖于 GitHub 原生的 `Pre-release` 标签。开发版发布并在经验证无异常后，会直接移除该标签使其无缝转为稳定版推送。
> 我们一直尝试在改进至更为优秀且高效的更新策略。
---

### UI 优化 (UI Improvements)
- **搜索框交互体验优化**
  **向左自然展开**：改进了标题栏搜索框的展开动画，改用右边缘对齐 (`BottomEdgeAlignedRight`) 向左侧延伸补全展开，在视觉上更为协调。
  **防误触收起机制**：增强了搜索浮窗交互，现在当搜索框内已有输入的文本时，点击外部区域将不会自动收缩关闭搜索框，避免中断正在进行的搜索和输入流程。
- **深浅色主题自适应优化**
  **原生的标题栏色彩控制**：移除了代码中不必要的硬编码深浅色强制定义，重构并全面交还给 WinUI3/WinAppSDK 底层管理。现在在系统设置中切换明亮或暗黑模式时，窗口控制按键能够免重启瞬间响应系统的色彩翻转。
  **更纯粹的更新提示表现**：精简了更新版本号与同步图标的提示逻辑。版本号基于原生 `ThemeResource` 机制完美跟随深浅色自适应；“发现新版本”的同步红点图标则回归纯粹的只显红色，全面减负并解决了切换主题过程中的样式冲突与异常。

### 功能增强 (Features)
- **跨版本更新日志堆叠展示**
  在此版本之后，如果客户端检测到发现了跨多个版本的更新（例如在使用一段时间未更新后从 1.0.9 直升至 1.0.13），更新弹窗不仅会显示最新版本的更新说明，还会**按倒序（从新到旧）自动串联合并期间所有跳过版本的发行日志**。让您彻底了解自从上次更新到目前最新版本期间所有的变更与新特性。