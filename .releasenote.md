本次更新提升了 GitHub 令牌的本地存储安全性，令牌现以加密形式存储于配置文件中。

---

## 🛠️ 优化

- **GitHub 令牌加密存储**：
  - 用户输入的 GitHub Personal Access Token 不再以明文写入配置文件
  - 令牌改用 Windows 系统级数据保护（DPAPI）加密后存储于 `settings.yaml`，仅当前 Windows 用户可解密
  - 已保存的旧版明文令牌将在首次启动时自动加密迁移，迁移后配置文件中的明文即被替换为密文

- **令牌输入框明文显示**：
  - 设置面板中的令牌输入框改为明文常显，便于核对输入内容
